apiVersion: v1
kind: Service
metadata:
  name: mind-base-backend
  namespace: mind-base
  labels:
    app.kubernetes.io/name: mind-base
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: mind-base
    app.kubernetes.io/component: backend
