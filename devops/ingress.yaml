apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mind-base-ingress
  namespace: mind-base
  labels:
    app.kubernetes.io/name: mind-base
  annotations:
    # 使用 nginx ingress controller
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    # WebSocket / SSE 支持（聊天流式返回）
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-buffering: "off"
spec:
  rules:
    - host: mind-base.example.com  # 替换为实际域名
      http:
        paths:
          # API 路由到后端
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: mind-base-backend
                port:
                  number: 8000
          - path: /docs
            pathType: Prefix
            backend:
              service:
                name: mind-base-backend
                port:
                  number: 8000
          - path: /health
            pathType: Exact
            backend:
              service:
                name: mind-base-backend
                port:
                  number: 8000
          # 其余路由到前端
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mind-base-frontend
                port:
                  number: 3000
